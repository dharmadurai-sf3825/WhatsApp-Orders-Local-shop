rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT MODE - Allow all reads and writes
    // ⚠️ WARNING: Change these rules before going to production!
    // 
    // For now, we allow all operations to test the app without authentication
    // In production, uncomment the authenticated rules below
    
    match /{document=**} {
      allow read, write: if true;  // 🔓 Open access for development
    }
    
    // PRODUCTION RULES (currently disabled - uncomment when ready)
    /*
    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }
    
    // Shops collection
    match /shops/{shopId} {
      allow read: if true; // Public read for customers
      allow write: if isOwner(resource.data.ownerId);
    }
    
    // Products collection
    match /products/{productId} {
      allow read: if true; // Public read for customers
      allow write: if isAuthenticated() && 
                     exists(/databases/$(database)/documents/shops/$(resource.data.shopId)) &&
                     get(/databases/$(database)/documents/shops/$(resource.data.shopId)).data.ownerId == request.auth.uid;
    }
    
    // Orders collection
    match /orders/{orderId} {
      allow read: if isOwner(resource.data.customerId) || 
                     isOwner(resource.data.sellerId);
      allow create: if isAuthenticated();
      allow update: if isOwner(resource.data.sellerId); // Only seller can update order status
      allow delete: if false; // No deletion allowed
    }
    
    // Payments collection
    match /payments/{paymentId} {
      allow read: if isOwner(resource.data.userId);
      allow create: if isAuthenticated();
      allow update, delete: if false; // Payments are immutable
    }
    */
  }
}
